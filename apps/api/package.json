{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "fastify start -l info -w src/app.ts",
    "build": "tsc",
    "start": "fastify start -l info src/app.ts",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "fastify": "^4.25.0",
    "@fastify/cors": "^8.5.0",
    "@fastify/jwt": "^8.0.0",
    "@prisma/client": "^5.8.0",
    "shared": "*",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0",
    "@types/bcryptjs": "^2.4.6",
    "prisma": "^5.8.0",
    "fastify-tsconfig": "^1.0.0",
    "ts-node": "^10.9.0"
  }
}
